/**
 * TestingSettingsTab — placeholder scaffold for Test Suite settings.
 *
 * Wave 3 of the Test Suite plan replaces this with real configuration
 * sections (default viewport, saved configs, screenshot retention, etc).
 */

import { PageContent } from '@ui';

export function TestingSettingsTab() {
  return (
    <PageContent>
      <div className="p-6 text-text-muted">Testing settings (placeholder)</div>
    </PageContent>
  );
}
