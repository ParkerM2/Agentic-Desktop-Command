/**
 * CommitPanel — STUB (Task #4 fills this in)
 *
 * Exports the prop interface so GitPage can type-check cleanly.
 */

export interface CommitPanelProps {
  repoPath: string;
  projectId: string;
}

export function CommitPanel(_props: CommitPanelProps) {
  return (
    <div className="text-muted-foreground p-4 text-sm">Commit panel coming soon...</div>
  );
}
